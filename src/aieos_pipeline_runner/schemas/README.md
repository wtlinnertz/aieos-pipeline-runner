# Vendored Schemas

Copies of frozen artifacts from `aieos-governance-foundation` at their v1.0
tags. Runtime-loaded by the pipeline runner for spec validation.

| File | Upstream | Frozen tag |
|---|---|---|
| `ci-spec.schema.json` | `schema/ci-spec.schema.json` | `v1.0-ci-spec-schema` |
| `cd-spec.schema.json` | `schema/cd-spec.schema.json` | `v1.0-cd-spec-schema` |
| `capability-contract.schema.json` | `schema/capability-contract.schema.json` | `v1.0-contracts` |

Update only on a formal upstream cutover.
